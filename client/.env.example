# Public Google OAuth client ID of type "Web application", required by Android
# Credential Manager. This is an OAuth identifier, not a browser app target.
# Create it in the same Google Cloud project as the Android package + SHA-1 client.
# npm run setup:android creates .env and the APK build configuration from this ID.
EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=
